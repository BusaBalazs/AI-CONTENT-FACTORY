services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n

    ports:
      - "${N8N_PORT}:5678"

    environment:
      TZ: ${TZ}

    volumes:
      - ./data/n8n:/home/node/.n8n

    restart: unless-stopped